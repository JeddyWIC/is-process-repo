# For local development, uses file:local.db (SQLite file)
# For production (Turso), set these in Netlify environment variables:
# TURSO_DATABASE_URL=libsql://your-db-name-your-org.turso.io
# TURSO_AUTH_TOKEN=your-auth-token

# Shared password for creating/editing processes
EDIT_PASSWORD=changeme
